spring:
  h2:
    console:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: actuator,info,health,metrics
  endpoint:
    health:
      show-details: always

info:
  animalFactsDateOfCreation: 11/30/2018